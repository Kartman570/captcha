set of captchas that hard to broke by bots/ai


### install
>python3 -m venv venv

>source venv/bin/activate

>pip3 install -r requirements.txt

### create requirements
>pip3 freeze > requirements.txt